#include "main.h"

/**
  * _atoi - converts string to int
  * @s : string to check
  *return: integer
  */

int _atoi(char *s)
{
	int i, len;
	char 

	len = 0;
	while(s[len] != '\0')
		len++;
	for (i = 0; i < len; i++)
	{
		if (s[i] >= '0' && s[i] <= '9' && i <= len)
		{

		}
	}
}
